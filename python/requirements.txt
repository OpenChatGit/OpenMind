torch
diffusers
transformers
accelerate
safetensors

# Optional: For GGUF model support (quantized Stable Diffusion)
# stable-diffusion-cpp-python

# For CUDA/GPU support:
# 1. Install PyTorch with CUDA: pip install torch --index-url https://download.pytorch.org/whl/cu121
# 2. For GGUF GPU support, install stable-diffusion-cpp-python with CUDA:
#    CMAKE_ARGS="-DSD_CUBLAS=ON" pip install stable-diffusion-cpp-python --force-reinstall --no-cache-dir
